# Android build artifacts & local-only files — but keep the Gradle wrapper so
# CI (GitHub Actions) can bootstrap the build without a local Gradle install.
/.gradle
/captures/
/local.properties
.cxx/

# Generated by `flutter pub get` / plugin registration at build time.
GeneratedPluginRegistrant.java

# Remember to never publicly share your keystore.
key.properties
**/*.keystore
**/*.jks
